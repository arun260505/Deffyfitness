[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so clean URLs (/programs, /contact …) work on reload
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
